{
  "name": "subql",
  "version": "1.0.7",
  "description": "subql index",
  "author": "Darwinia Network",
  "license": "MIT",
  "private": true,
  "scripts": {
    "boot": "yarn && lerna bootstrap",
    "build:common": "cd common && yarn && yarn build",
    "build:apps": "lerna run build --scope subql-apps --",
    "build:bridger": "lerna run build --scope subql-bridger --",
    "build:mmr": "lerna run build --scope subql-mmr --",
    "publish:apps": "lerna run publish --scope subql-apps --",
    "publish:bridger": "lerna run publish --scope subql-bridger --",
    "publish:mmr": "lerna run publish --scope subql-mmr --"
  },
  "devDependencies": {
    "lerna": "^5.5.2",
    "nx": "^14.4.3"
  }
}
